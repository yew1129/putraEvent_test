<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyAppTest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyAppTest" parent="Base.Theme.MyAppTest" />

    <!-- New style defined for the time picker -->
    <style name="TimePickerTheme" parent="ThemeOverlay.MaterialComponents.TimePicker">
        <!-- Color of the selected option in the picker -->
        <item name="colorPrimary">@color/pink_dark</item>
    </style>

    <!-- New style defined for the date picker -->
    <style name="DatePickerTheme" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <!-- Color of the selected option in the picker -->
        <item name="colorPrimary">@color/pink_dark</item>
    </style>
</resources>